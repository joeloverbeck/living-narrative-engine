/**
 * @file Defines keys used for storing and retrieving data in the game data registry.
 */

/**
 * Registry key for storing loaded scope definitions.
 *
 * @type {string}
 */
export const SCOPES_KEY = 'scopes';

/**
 * Registry key for storing loaded lookup definitions.
 *
 * @type {string}
 */
export const LOOKUPS_KEY = 'lookups';
