// jest.config.js
export default {
    // Jest will automatically pick up on "type": "module" in package.json
    // and use the Node.js experimental ESM support.
    // No specific transform is needed for basic ESM syntax if Node supports it.
    // testEnvironment: 'node', // Already the default for Jest
};