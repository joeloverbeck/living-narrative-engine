T-13 Filter & Union Resolvers
Implement remaining node resolvers using gateways.

Files

scopeDsl/nodes/filterResolver.js

scopeDsl/nodes/unionResolver.js

tests.

filterResolver factory params

({ logicEval, entitiesGateway, locationProvider })
Use evaluator exactly as original engine.

unionResolver simply unions results of left/right using new Set([...a,...b]).