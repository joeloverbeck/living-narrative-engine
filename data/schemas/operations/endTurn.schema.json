{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/endTurn.schema.json",
  "title": "END_TURN Operation",
  "allOf": [
    {
      "$ref": "../base-operation.schema.json"
    },
    {
      "properties": {
        "type": {
          "const": "END_TURN"
        },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for the END_TURN operation, dispatching core:turn_ended.",
      "properties": {
        "entityId": {
          "type": "string",
          "minLength": 1
        },
        "success": {
          "type": "boolean",
          "description": "Execution integrity indicator. True means the rule executed without runtime errors. This is independent of whether the action achieved its narrative goal - a FAILURE or FUMBLE outcome should still use success=true because the rule logic executed correctly."
        },
        "error": {
          "type": "object"
        }
      },
      "required": ["entityId", "success"],
      "additionalProperties": false
    }
  }
}
