{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/applyDamage.schema.json",
  "title": "APPLY_DAMAGE Operation",
  "description": "Applies direct damage to a specific part of an entity, updating its health, handling thresholds/status labels, and dispatching events.",
  "allOf": [
    { "$ref": "../base-operation.schema.json" },
    {
      "properties": {
        "type": { "const": "APPLY_DAMAGE" },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for the APPLY_DAMAGE operation.",
      "properties": {
        "entity_ref": {
          "description": "Reference to the target entity (the creature/character). Can be a string or JSON Logic.",
          "oneOf": [
            { "type": "string" },
            { "type": "object" }
          ]
        },
        "part_ref": {
          "description": "Reference to the specific body part entity. Optional. If missing, logic will attempt to resolve a hit location. Can be a string or JSON Logic.",
          "oneOf": [
            { "type": "string" },
            { "type": "object" }
          ]
        },
        "amount": {
          "description": "Amount of damage to apply. Must be positive. Can be a number or JSON Logic expression.",
          "oneOf": [
            { "type": "number" },
            { "type": "object" }
          ]
        },
        "damage_type": {
          "description": "Type of damage (e.g., 'blunt', 'piercing', 'slashing'). Can be a string or JSON Logic.",
          "oneOf": [
            { "type": "string" },
            { "type": "object" }
          ]
        }
      },
      "required": ["entity_ref", "amount", "damage_type"],
      "additionalProperties": false
    }
  }
}
