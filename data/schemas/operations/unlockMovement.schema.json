{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/unlockMovement.schema.json",
  "title": "UNLOCK_MOVEMENT Operation",
  "allOf": [
    {
      "$ref": "../base-operation.schema.json"
    },
    {
      "properties": {
        "type": {
          "const": "UNLOCK_MOVEMENT"
        },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for the UNLOCK_MOVEMENT operation. Unlocks movement for the specified actor.",
      "properties": {
        "actor_id": {
          "type": "string",
          "description": "The ID of the actor to unlock movement for"
        }
      },
      "required": ["actor_id"],
      "additionalProperties": false
    }
  }
}
