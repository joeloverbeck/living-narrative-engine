{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/restoreOxygen.schema.json",
  "title": "RESTORE_OXYGEN Operation",
  "description": "Restores oxygen to an entity's respiratory organs. Used by drowning/asphyxiation mechanics.",
  "allOf": [
    { "$ref": "../base-operation.schema.json" },
    {
      "properties": {
        "type": { "const": "RESTORE_OXYGEN" },
        "parameters": { "$ref": "#/$defs/Parameters" }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for the RESTORE_OXYGEN operation.",
      "properties": {
        "entityId": {
          "$ref": "../common.schema.json#/definitions/entityReference",
          "description": "Reference to the entity whose respiratory organs will have oxygen restored."
        },
        "restoreFull": {
          "type": "boolean",
          "description": "When true, restores oxygen to full capacity."
        },
        "amount": {
          "type": "integer",
          "description": "Amount of oxygen to restore when not restoring to full capacity."
        }
      },
      "required": ["entityId"],
      "additionalProperties": false
    }
  }
}
