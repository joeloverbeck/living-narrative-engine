{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/validateContainerCapacity.schema.json",
  "title": "VALIDATE_CONTAINER_CAPACITY Operation",
  "allOf": [
    {
      "$ref": "../base-operation.schema.json"
    },
    {
      "properties": {
        "type": {
          "const": "VALIDATE_CONTAINER_CAPACITY"
        },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for the VALIDATE_CONTAINER_CAPACITY operation. Validates whether a container has sufficient capacity to hold an item.",
      "properties": {
        "containerEntity": {
          "type": "string",
          "minLength": 1,
          "pattern": "^\\S(.*\\S)?$",
          "description": "Entity ID of the container whose capacity is being validated"
        },
        "itemEntity": {
          "type": "string",
          "minLength": 1,
          "pattern": "^\\S(.*\\S)?$",
          "description": "Entity ID of the item to be added to container"
        },
        "result_variable": {
          "type": "string",
          "minLength": 1,
          "description": "Variable name to store validation result object with 'valid' boolean and optional 'reason' string"
        }
      },
      "required": ["containerEntity", "itemEntity", "result_variable"],
      "additionalProperties": false
    }
  }
}
