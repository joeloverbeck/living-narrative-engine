{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/updateHungerState.schema.json",
  "title": "UPDATE_HUNGER_STATE Operation",
  "description": "Calculates hunger state from energy thresholds and updates hunger state component with energy percentage and turns in state",
  "allOf": [
    {
      "$ref": "../base-operation.schema.json"
    },
    {
      "properties": {
        "type": {
          "const": "UPDATE_HUNGER_STATE"
        },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "properties": {
        "entity_ref": {
          "description": "Reference to the entity with hunger_state and metabolic_store components",
          "oneOf": [{ "type": "string" }, { "type": "object" }]
        }
      },
      "required": ["entity_ref"],
      "additionalProperties": false
    }
  }
}
