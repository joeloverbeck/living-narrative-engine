{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema://living-narrative-engine/operations/breakClosenessWithTarget.schema.json",
  "title": "BREAK_CLOSENESS_WITH_TARGET Operation",
  "allOf": [
    {
      "$ref": "../base-operation.schema.json"
    },
    {
      "properties": {
        "type": {
          "const": "BREAK_CLOSENESS_WITH_TARGET"
        },
        "parameters": {
          "$ref": "#/$defs/Parameters"
        }
      }
    }
  ],
  "$defs": {
    "Parameters": {
      "type": "object",
      "description": "Parameters for breaking closeness relationship between actor and specific target",
      "properties": {
        "actor_id": {
          "description": "The actor breaking closeness with the target",
          "type": "string",
          "minLength": 1
        },
        "target_id": {
          "description": "The target to break closeness with",
          "type": "string",
          "minLength": 1
        },
        "result_variable": {
          "description": "Optional variable name to store operation result for rule conditions",
          "type": "string",
          "minLength": 1
        }
      },
      "required": ["actor_id", "target_id"],
      "additionalProperties": false
    }
  }
}
