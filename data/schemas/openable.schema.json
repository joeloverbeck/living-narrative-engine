{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://example.com/schemas/openable.schema.json",
  "title": "Openable Component Schema",
  "description": "Defines the state for an entity that can be opened or closed.",
  "type": "object",
  "properties": {
    "isOpen": {
      "type": "boolean",
      "description": "Indicates whether the entity is currently open (true) or closed (false).",
      "default": false
    }
  },
  "required": [
    "isOpen"
  ],
  "additionalProperties": false
}