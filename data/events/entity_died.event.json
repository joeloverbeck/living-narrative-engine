{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "event:entity_died",
  "description": "Signals that an entity's health has reached zero or below as a result of an action (typically combat).",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "deceasedEntityId": {
        "type": "string",
        "description": "The unique identifier of the entity that died.",
        "$ref": "./common.schema.json#/definitions/namespacedId"
      },
      "killerEntityId": {
        "description": "The unique identifier of the entity considered responsible for the death (e.g., the attacker), or null if cause is environmental/indirect.",
        "$ref": "./common.schema.json#/definitions/nullableNamespacedId"
      }
    },
    "required": [
      "deceasedEntityId",
      "killerEntityId"
    ],
    "additionalProperties": false
  }
}