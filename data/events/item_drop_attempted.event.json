{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "event:item_drop_attempted",
  "description": "Signals that a player has validated their intent to drop a specific item from their inventory into their current location.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "playerId": {
        "type": "string",
        "description": "The unique identifier of the player entity attempting to drop the item.",
        "$ref": "./common.schema.json#/definitions/namespacedId"
      },
      "itemInstanceId": {
        "type": "string",
        "description": "The unique identifier of the specific item instance being dropped.",
        "$ref": "./common.schema.json#/definitions/namespacedId"
      },
      "locationId": {
        "type": "string",
        "description": "The unique identifier of the location entity where the item is intended to be dropped.",
        "$ref": "./common.schema.json#/definitions/namespacedId"
      }
    },
    "required": [
      "playerId",
      "itemInstanceId",
      "locationId"
    ],
    "additionalProperties": false
  }
}