{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "event:look_intended",
  "description": "Signals intent to get information about a general scope (location, self, or specific target).",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "actorId": {
        "type": "string",
        "description": "The unique ID of the entity initiating the look action (usually player).",
        "$ref": "http://example.com/schemas/common.schema.json#/definitions/namespacedId"
      },
      "scope": {
        "type": "string",
        "description": "The scope of the intended look action.",
        "enum": [
          "location",
          "self",
          "target"
        ]
      },
      "targetEntityId": {
        "description": "The unique ID of the specific entity being looked at, only present when scope is 'target'. Null otherwise.",
        "$ref": "http://example.com/schemas/common.schema.json#/definitions/nullableNamespacedId"
      }
    },
    "required": [
      "actorId",
      "scope",
      "targetEntityId"
    ],
    "additionalProperties": false
  }
}