{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "event:unlock_entity_force",
  "description": "Used by cut-scenes, triggers, or scripts to unlock an entity without consuming/validating a key.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "targetEntityId": {
        "type": "string",
        "description": "The entity to unlock.",
        "$ref": "./common.schema.json#/definitions/namespacedId"
      },
      "userId": {
        "description": "Optional. Who caused it (null for world events).",
        "$ref": "./common.schema.json#/definitions/nullableNamespacedId"
      },
      "force": {
        "type": "boolean",
        "description": "Optional. Always true – lets analytics separate forced unlocks from player actions.",
        "const": true
      }
    },
    "required": [
      "targetEntityId"
    ],
    "additionalProperties": false
  }
}