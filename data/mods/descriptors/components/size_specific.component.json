{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "descriptors:size_specific",
  "description": "Context-specific size descriptors (e.g., cup sizes for anatomy, heel heights for footwear, lengths for jewelry). Note: Domain-specific validation (such as restricting to specific cup sizes) should be applied at the usage level (recipes, rules, or domain-specific validators) rather than in this generic component schema.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "size": {
        "type": "string",
        "description": "The specific size descriptor (context-dependent)",
        "minLength": 1
      }
    },
    "required": ["size"],
    "additionalProperties": false
  },
  "validationRules": {
    "generateValidator": true,
    "errorMessages": {
      "missingRequired": "Size descriptor is required"
    }
  }
}
