{
  "$schema": "http://example.com/schemas/component.schema.json",
  "description": "Basic color descriptors",
  "data": {
    "type": "object",
    "properties": {
      "color": {
        "type": "string",
        "description": "The basic color",
        "enum": ["red", "blue", "green", "yellow", "orange", "purple", "brown", "black", "white", "gray"],
        "default": "brown"
      }
    },
    "required": ["color"],
    "additionalProperties": false
  }
}