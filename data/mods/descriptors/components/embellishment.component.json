{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "descriptors:embellishment",
  "description": "Structured descriptors for decorative embellishments applied to garments and accessories",
  "dataSchema": {
    "type": "object",
    "properties": {
      "embellishment": {
        "type": "string",
        "description": "The primary embellishment featured on the item (plural form preferred for natural language formatting)",
        "enum": ["crystals", "gemstones", "metal chains", "pearls", "spikes", "silver-hoops", "charms"]
      }
    },
    "required": ["embellishment"],
    "additionalProperties": false
  },
  "validationRules": {
    "generateValidator": true,
    "errorMessages": {
      "invalidEnum": "Invalid embellishment: {{value}}. Valid options: {{validValues}}",
      "missingRequired": "Embellishment is required",
      "invalidType": "Invalid type for embellishment: expected {{expected}}, got {{actual}}"
    },
    "suggestions": {
      "enableSimilarity": true,
      "maxDistance": 3,
      "maxSuggestions": 3
    }
  }
}
