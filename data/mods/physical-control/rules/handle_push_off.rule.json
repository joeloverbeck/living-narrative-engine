{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_push_off",
  "comment": "Handles the 'physical-control:push_off' action. Removes closeness between entities, dispatches descriptive text, and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "physical-control:event-is-action-push-off"
  },
  "actions": [
    {
      "type": "GET_NAME",
      "parameters": {
        "entity_ref": "actor",
        "result_variable": "actorName"
      }
    },
    {
      "type": "GET_NAME",
      "parameters": {
        "entity_ref": "target",
        "result_variable": "targetName"
      }
    },
    {
      "type": "QUERY_COMPONENT",
      "parameters": {
        "entity_ref": "actor",
        "component_type": "core:position",
        "result_variable": "actorPosition"
      }
    },
    {
      "type": "BREAK_CLOSENESS_WITH_TARGET",
      "comment": "Break closeness relationship between actor and target with conditional removal",
      "parameters": {
        "actor_id": "{event.payload.actorId}",
        "target_id": "{event.payload.targetId}"
      }
    },
    {
      "type": "DISPATCH_PERCEPTIBLE_EVENT",
      "comment": "Dispatch sense-aware perceptible event for pushing off.",
      "parameters": {
        "location_id": "{context.actorPosition.locationId}",
        "description_text": "{context.actorName} pushes {context.targetName} off forcefully, breaking their closeness.",
        "actor_description": "I push {context.targetName} off forcefully, breaking our closeness.",
        "target_description": "{context.actorName} pushes me off forcefully, breaking our closeness.",
        "perception_type": "physical.target_action",
        "actor_id": "{event.payload.actorId}",
        "target_id": "{event.payload.targetId}",
        "alternate_descriptions": {
          "auditory": "I hear the sounds of a struggle and someone being pushed away nearby.",
          "tactile": "I feel vibrations as someone is pushed away nearby."
        }
      }
    },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} pushes {context.targetName} off forcefully, breaking their closeness."
      }
    },
    {
      "macro": "core:logSuccessOutcomeAndEndTurn"
    }
  ]
}
