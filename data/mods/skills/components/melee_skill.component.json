{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:melee_skill",
  "description": "Melee combat proficiency. Determines effectiveness in close-quarters weapon combat.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 10,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
