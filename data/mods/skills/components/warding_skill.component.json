{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:warding_skill",
  "description": "Proficiency in creating protective wards and barriers. Determines effectiveness in warding rituals and salt boundary creation.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 10,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
