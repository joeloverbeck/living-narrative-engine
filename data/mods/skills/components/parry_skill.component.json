{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:parry_skill",
  "description": "Parry skill. Determines ability to deflect attacks with weapons or shields.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
