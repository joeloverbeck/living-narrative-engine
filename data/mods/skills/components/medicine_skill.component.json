{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:medicine_skill",
  "description": "Medical proficiency for diagnosing and treating wounds.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Skill level from 0 (untrained) to 100 (expert)."
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
