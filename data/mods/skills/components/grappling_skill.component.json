{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:grappling_skill",
  "description": "Grappling competency used for physical grapples and restraints.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
