{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:dodge_skill",
  "description": "Evasion skill. Determines ability to dodge incoming attacks through agility and movement.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
