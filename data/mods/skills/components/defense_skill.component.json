{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "skills:defense_skill",
  "description": "Defensive combat skill. Determines ability to avoid or reduce damage from incoming attacks.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Skill level from 0 (untrained) to 100 (master)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
