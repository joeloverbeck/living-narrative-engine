{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "blockers:structural_resistance",
  "description": "Structural resistance level for an inanimate object.",
  "dataSchema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "integer",
        "minimum": 0,
        "maximum": 100,
        "default": 0,
        "description": "Resistance level from 0 (none) to 100 (max)"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  }
}
