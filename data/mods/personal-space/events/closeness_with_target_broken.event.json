{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "personal-space:closeness_with_target_broken",
  "description": "Dispatched when a closeness relationship between an actor and a target is forcefully broken.",
  "payloadSchema": {
    "title": "Personal Space: Closeness With Target Broken Payload",
    "description": "Payload for the personal-space:closeness_with_target_broken event.",
    "type": "object",
    "properties": {
      "actorId": {
        "$ref": "schema://living-narrative-engine/common.schema.json#/definitions/namespacedId",
        "description": "The ID of the actor who is breaking closeness with the target."
      },
      "targetId": {
        "$ref": "schema://living-narrative-engine/common.schema.json#/definitions/namespacedId",
        "description": "The ID of the target entity from whom closeness is being broken."
      },
      "operationId": {
        "type": "string",
        "description": "Unique identifier for this operation instance."
      }
    },
    "required": ["actorId", "targetId", "operationId"],
    "additionalProperties": false
  }
}
