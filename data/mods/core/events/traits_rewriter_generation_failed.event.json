{
  "$schema": "schema://living-narrative-engine/event.schema.json",
  "id": "core:traits_rewriter_generation_failed",
  "description": "Dispatched when traits rewriter generation fails.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "characterName": {
        "description": "The name of the character that failed to be processed",
        "type": "string"
      },
      "error": {
        "description": "Error message describing what went wrong",
        "type": "string"
      },
      "processingTime": {
        "description": "Time taken before failure in milliseconds",
        "type": "number"
      },
      "timestamp": {
        "description": "ISO 8601 timestamp of when generation failed",
        "type": "string",
        "format": "date-time"
      },
      "errorType": {
        "description": "The type/class of error that occurred",
        "type": "string"
      },
      "errorMessage": {
        "description": "The specific error message",
        "type": "string"
      },
      "options": {
        "description": "Generation options that were used",
        "type": "object",
        "additionalProperties": true
      }
    },
    "required": ["error", "timestamp"],
    "additionalProperties": true
  }
}
