{
  "$schema": "http://example.com/schemas/event.schema.json",
  "id": "core:speech_input_gained_focus",
  "description": "Fired when the speech input element gains focus, indicating the human player is about to type.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "timestamp": {
        "type": "number",
        "description": "The timestamp when the input gained focus"
      }
    },
    "additionalProperties": false
  }
}
