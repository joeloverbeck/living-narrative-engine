{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "core:new_game_started",
  "description": "Dispatched when a new game is started successfully, after the initial world has been loaded and basic engine systems are ready. This event signals that the game is now actively running from a fresh state.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "worldName": {
        "type": "string",
        "description": "The name of the world that was loaded for the new game."
      }
    },
    "required": [
      "worldName"
    ],
    "additionalProperties": false
  }
}