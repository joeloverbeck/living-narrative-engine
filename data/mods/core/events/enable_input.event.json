{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "core:enable_input",
  "description": "Signals the UI to enable the primary command input field, optionally setting its placeholder text.",
  "payloadSchema": {
    "type": "object",
    "description": "Payload for the core:enable_input event.",
    "properties": {
      "placeholder": {
        "description": "Optional. The placeholder text to display in the input field when it's empty. Defaults to 'Enter command...'.",
        "type": "string",
        "default": "Enter command..."
      }
    },
    "required": [],
    "additionalProperties": false,
    "$comment": "This event tells the UI that the game is ready for the next command input. The placeholder provides guidance."
  }
}
