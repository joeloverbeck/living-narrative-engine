{
  "$schema": "schema://living-narrative-engine/event.schema.json",
  "id": "core:cliches_stored",
  "description": "Dispatched when clichés are successfully stored.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "conceptId": {
        "description": "Unique identifier of the character concept",
        "type": "string"
      },
      "directionId": {
        "description": "Unique identifier of the thematic direction",
        "type": "string"
      },
      "clicheId": {
        "description": "Unique identifier of the stored cliché",
        "type": "string"
      },
      "count": {
        "description": "Total number of clichés now stored",
        "type": "integer",
        "minimum": 0
      }
    },
    "required": ["conceptId", "directionId"],
    "additionalProperties": true
  }
}
