{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "core:ai_turn_processing_started",
  "description": "Fired just before the game begins processing an AI entity's turn.",
  "payloadSchema": {
    "title": "Core: AI Turn Processing Started Event Payload",
    "description": "Defines the payload structure for the 'core:ai_turn_processing_started' event.",
    "type": "object",
    "properties": {
      "entityId": {
        "$ref": "../../../schemas/common.schema.json#/definitions/namespacedId",
        "description": "The unique ID of the AI entity whose turn processing is starting."
      }
    },
    "required": [
      "entityId"
    ],
    "additionalProperties": false
  }
}