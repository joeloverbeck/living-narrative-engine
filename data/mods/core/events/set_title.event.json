{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "core:set_title",
  "description": "Signals a request to update the main title display in the user interface, often used to indicate game state, loading progress, or the current world/area name.",
  "payloadSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The text content to display as the new title."
      }
    },
    "required": ["text"],
    "additionalProperties": false,
    "$comment": "Payload contains only the text string for the new title."
  }
}
