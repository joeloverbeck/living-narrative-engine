{
  "$schema": "schema://living-narrative-engine/event.schema.json",
  "id": "core:damage_simulator_execution_error",
  "description": "Fired when damage execution fails in the damage simulator.",
  "payloadSchema": {
    "type": "object",
    "required": ["entityId", "error"],
    "properties": {
      "entityId": {
        "type": "string",
        "description": "The ID of the entity that was being damaged."
      },
      "error": {
        "type": "string",
        "description": "Error message describing what went wrong."
      }
    },
    "additionalProperties": false
  }
}
