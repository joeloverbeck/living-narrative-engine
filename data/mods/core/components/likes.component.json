{
  "$schema": "http://example.com/schemas/component-definition.schema.json",
  "id": "core:likes",
  "description": "Stores a list of things the character likes, typically as a delimited string.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "text"
    ],
    "properties": {
      "text": {
        "type": "string",
        "description": "A string listing likes (e.g., 'mathematics + music + genuine conversation')."
      }
    }
  }
}