{
  "$schema": "http://example.com/schemas/component.schema.json",
  "id": "core:player",
  "description": "A tag component indicating that the entity represents a player. Used by systems and conditions to identify player-controlled entities.",
  "dataSchema": {
    "type": "object",
    "description": "This is a tag component and holds no data. Its presence alone signifies the entity is a player.",
    "properties": {},
    "additionalProperties": false
  }
}
