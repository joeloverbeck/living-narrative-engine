{
  "$schema": "../../../schemas/component.schema.json",
  "id": "anatomy:joint",
  "description": "Represents a joint connection between a child body part and its parent via a socket",
  "dataSchema": {
    "type": "object",
    "properties": {
      "parentId": {
        "type": "string",
        "description": "Runtime instance ID of the parent body part entity"
      },
      "socketId": {
        "type": "string",
        "description": "ID of the socket on the parent where this part is attached"
      },
      "jointType": {
        "type": "string",
        "enum": ["hinge", "ball", "suture", "fused"],
        "description": "Type of joint connection inherited from the socket definition"
      },
      "breakThreshold": {
        "type": "integer",
        "minimum": 0,
        "default": 0,
        "description": "Damage threshold required to sever this connection"
      }
    },
    "required": ["parentId", "socketId"],
    "additionalProperties": false
  }
}