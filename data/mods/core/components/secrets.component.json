{
  "$schema": "http://example.com/schemas/component-definition.schema.json",
  "id": "core:secrets",
  "description": "Stores the character's secrets, typically as a detailed string or delimited list.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "text"
    ],
    "properties": {
      "text": {
        "type": "string",
        "description": "A string detailing the character's secrets."
      }
    }
  }
}