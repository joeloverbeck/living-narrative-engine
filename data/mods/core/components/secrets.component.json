{
  "$schema": "http://example.com/schemas/component.schema.json",
  "id": "core:secrets",
  "description": "Stores the character's secrets, typically as a detailed string or delimited list.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["text"],
    "properties": {
      "text": {
        "type": "string",
        "description": "A string detailing the character's secrets, written from their first-person perspective, as if the character is confiding them. For example: 'There's something I've never told anyone: ...' or 'My secrets include: 1. X, 2. Y.'."
      }
    }
  }
}
