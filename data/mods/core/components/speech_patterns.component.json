{
  "$schema": "http://example.com/schemas/component-definition.schema.json",
  "id": "core:speech_patterns",
  "description": "Stores examples of the character's typical speech patterns or example phrases.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "patterns"
    ],
    "properties": {
      "patterns": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "An array of strings, where each string is an example of the character's speech."
      }
    }
  }
}