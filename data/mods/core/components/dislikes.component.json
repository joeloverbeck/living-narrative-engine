{
  "$schema": "http://example.com/schemas/component-definition.schema.json",
  "id": "core:dislikes",
  "description": "Stores a list of things the character dislikes, typically as a delimited string.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "text"
    ],
    "properties": {
      "text": {
        "type": "string",
        "description": "A string listing dislikes (e.g., 'social pretense + superficial therapy + war')."
      }
    }
  }
}