{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "core:position",
  "description": "Represents the entity's location within the game world.",
  "dataSchema": {
    "description": "Schema for validating PositionComponent data.",
    "type": "object",
    "properties": {
      "locationId": {
        "$ref": "schema://living-narrative-engine/common.schema.json#/definitions/namespacedId",
        "description": "The ID of the location entity where the entity resides. This will be resolved to an instance ID during world initialization."
      }
    },
    "required": ["locationId"],
    "additionalProperties": false
  }
}
