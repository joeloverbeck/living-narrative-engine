{
  "$schema": "http://example.com/schemas/condition.schema.json",
  "id": "core:target-is-not-self",
  "description": "Checks if the target's ID is different from the actor's ID.",
  "logic": {
    "!=": [
      {
        "var": "actor.id"
      },
      {
        "var": "target.id"
      }
    ]
  }
}
