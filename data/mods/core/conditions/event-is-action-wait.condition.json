{
  "$schema": "http://example.com/schemas/condition.schema.json",
  "id": "core:event-is-action-wait",
  "description": "Checks if the triggering event is for the 'core:wait' action.",
  "logic": {
    "==": [
      {
        "var": "event.payload.actionId"
      },
      "core:wait"
    ]
  }
}