{
  "$schema": "schema://living-narrative-engine/condition.schema.json",
  "id": "patrol:blocker-is-dimensional-portal",
  "description": "Checks if the blocker is a dimensional portal by matching the blocker ID pattern",
  "logic": {
    "in": [
      "dimensional_rift_blocker",
      { "var": "entity.blocker" }
    ]
  }
}
