{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "positioning:giving_blowjob",
  "description": "Marks an actor who is actively performing oral sex on another entity's penis, preventing conflicting mouth engagement actions.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["receiving_entity_id", "initiated"],
    "properties": {
      "receiving_entity_id": {
        "type": "string",
        "description": "The ID of the entity receiving oral sex",
        "pattern": "^([a-zA-Z0-9_]+:[a-zA-Z0-9_-]+|[a-zA-Z0-9_]+)$"
      },
      "initiated": {
        "type": "boolean",
        "description": "Whether this entity initiated the blowjob interaction"
      },
      "consented": {
        "type": "boolean",
        "description": "Whether the giving entity consents to continue the interaction",
        "default": true
      }
    }
  }
}
