{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_kiss_neck_sensually",
  "comment": "Handles the 'kissing:kiss_neck_sensually' action. Dispatches descriptive text and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "kissing:event-is-action-kiss-neck-sensually"
  },
  "actions": [
    {
      "type": "PREPARE_ACTION_CONTEXT",
      "parameters": {}
    },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} kisses {context.targetName}'s neck sensually."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
