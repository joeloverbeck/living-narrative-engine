{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_aim_item",
  "comment": "Handles the aim_item action by adding aiming:aimed_at component to the item and dispatching item_aimed event",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "aiming:event-is-action-aim-item"
  },
  "actions": [
    {
      "type": "GET_TIMESTAMP",
      "comment": "Get current game timestamp for aim tracking",
      "parameters": {
        "result_variable": "currentTimestamp"
      }
    },
    {
      "type": "ADD_COMPONENT",
      "comment": "Add aiming:aimed_at to the item (secondary target)",
      "parameters": {
        "entity_ref": "{event.payload.secondaryId}",
        "component_type": "aiming:aimed_at",
        "value": {
          "targetId": "{event.payload.targetId}",
          "aimedBy": "{event.payload.actorId}"
        }
      }
    },
    {
      "type": "DISPATCH_EVENT",
      "comment": "Dispatch aiming:item_aimed event",
      "parameters": {
        "eventType": "aiming:item_aimed",
        "payload": {
          "actorEntity": "{event.payload.actorId}",
          "itemEntity": "{event.payload.secondaryId}",
          "targetEntity": "{event.payload.targetId}",
          "timestamp": "{context.currentTimestamp}"
        }
      }
    },
    {
      "type": "END_TURN",
      "comment": "Complete actor's turn successfully",
      "parameters": {
        "entityId": "{event.payload.actorId}",
        "success": true
      }
    }
  ]
}
