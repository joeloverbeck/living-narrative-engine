{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_feel_arm_muscles",
  "comment": "Handles the 'caressing:feel_arm_muscles' action. Dispatches descriptive text and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "caressing:event-is-action-feel-arm-muscles"
  },
  "actions": [
    { "type": "PREPARE_ACTION_CONTEXT", "parameters": {} },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} feels the hard swell of {context.targetName}'s muscles."
      }
    },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "perceptionEventMessage",
        "value": "{context.actorName} has felt the hard swell of {context.targetName}'s muscles."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
