{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_brush_hair_behind_ear",
  "comment": "Handles the 'affection:brush_hair_behind_ear' action. Dispatches descriptive text and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "affection:event-is-action-brush-hair-behind-ear"
  },
  "actions": [
    { "type": "PREPARE_ACTION_CONTEXT", "parameters": {} },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} brushes a stray lock of hair behind {context.targetName}'s ear."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
