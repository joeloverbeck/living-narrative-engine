{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_massage_shoulders",
  "comment": "Handles the 'affection:massage_shoulders' action. Dispatches descriptive text and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "affection:event-is-action-massage-shoulders"
  },
  "actions": [
    { "type": "PREPARE_ACTION_CONTEXT", "parameters": {} },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} kneads {context.targetName}'s shoulders."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
