{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_pat_ass_affectionately",
  "comment": "Handles the 'affection:pat_ass_affectionately' action and ends the turn with an affectionate log.",
  "event_type": "core:attempt_action",
  "condition": {
    "condition_ref": "affection:event-is-action-pat-ass-affectionately"
  },
  "actions": [
    { "type": "PREPARE_ACTION_CONTEXT", "parameters": {} },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} pats {context.targetName}'s ass affectionately."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
