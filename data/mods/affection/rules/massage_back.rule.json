{
  "$schema": "schema://living-narrative-engine/rule.schema.json",
  "rule_id": "handle_massage_back",
  "comment": "Handles the 'affection:massage_back' action. Dispatches descriptive text and ends the turn.",
  "event_type": "core:attempt_action",
  "condition": { "condition_ref": "affection:event-is-action-massage-back" },
  "actions": [
    { "type": "PREPARE_ACTION_CONTEXT", "parameters": {} },
    {
      "type": "SET_VARIABLE",
      "parameters": {
        "variable_name": "logMessage",
        "value": "{context.actorName} gently massages {context.targetName}'s back with smooth, soothing strokes."
      }
    },
    { "macro": "core:logSuccessAndEndTurn" }
  ]
}
