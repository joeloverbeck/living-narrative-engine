{
  "$schema": "schema://living-narrative-engine/condition.schema.json",
  "id": "bending-states:entity-is-bending-over",
  "description": "Checks if the entity is currently bending over (has the bending_over component).",
  "logic": {
    "!!": {
      "var": "entity.components.bending-states:bending_over"
    }
  }
}
