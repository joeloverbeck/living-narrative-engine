{
  "$schema": "http://example.com/schemas/event-definition.schema.json",
  "id": "textUI:update_available_actions",
  "description": "Signals a UI component (like the action buttons renderer) to update the display of currently available player actions based on the provided list of action objects.",
  "payloadSchema": {
    "type": "object",
    "title": "UIUpdateActionsPayload",
    "description": "Defines the payload for the 'textUI:update_available_actions' event, containing the list of available action objects.",
    "properties": {
      "actions": {
        "type": "array",
        "description": "An array of objects, each representing a valid action the player can currently take, including its ID and formatted command.",
        "items": {
          "type": "object",
          "description": "Object containing the action ID and its formatted command string.",
          "properties": {
            "id": {
              "$ref": "http://example.com/schemas/common.schema.json#/definitions/namespacedId",
              "description": "The unique ID of the action definition (e.g., 'core:wait')."
            },
            "command": {
              "type": "string",
              "description": "The formatted command string ready for display/parsing (e.g., 'wait', 'go north').",
              "minLength": 1
            }
          },
          "required": [
            "id",
            "command"
          ],
          "additionalProperties": false
        },
        "uniqueItems": false
      }
    },
    "required": [
      "actions"
    ],
    "additionalProperties": false
  }
}