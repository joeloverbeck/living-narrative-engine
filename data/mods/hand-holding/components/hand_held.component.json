{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "hand-holding:hand_held",
  "description": "Marks an entity whose hand is currently being held by another actor.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["holding_entity_id"],
    "properties": {
      "holding_entity_id": {
        "type": "string",
        "description": "The ID of the entity that is holding this actor's hand",
        "pattern": "^([a-zA-Z0-9_]+:[a-zA-Z0-9_-]+|[a-zA-Z0-9_]+)$"
      },
      "consented": {
        "type": "boolean",
        "description": "Whether this entity actively consented to the hand-holding interaction",
        "default": true
      }
    }
  }
}
