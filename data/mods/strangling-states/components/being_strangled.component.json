{
  "$schema": "schema://living-narrative-engine/component.schema.json",
  "id": "strangling-states:being_strangled",
  "description": "Indicates the actor is being strangled.",
  "dataSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "activityMetadata": {
        "type": "object",
        "description": "Inline metadata for activity description generation.",
        "additionalProperties": false,
        "properties": {
          "shouldDescribeInActivity": {
            "type": "boolean",
            "default": true
          },
          "template": {
            "type": "string",
            "default": "{actor} is being strangled"
          },
          "priority": {
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "default": 85
          }
        }
      }
    }
  }
}
